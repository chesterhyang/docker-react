language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t chesterhyang/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true chesterhyang/docker-react npm run test -- --coverage 